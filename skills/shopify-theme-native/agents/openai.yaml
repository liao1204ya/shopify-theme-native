interface:
  display_name: "Shopify 主题原生融合开发"
  short_description: "优先沿用主题字体布局，保留公共页头页脚，构建可配置模块"
  default_prompt: "请使用 $shopify-theme-native，以跟随目标主题字体布局、保留现有公共 header/footer 为最高默认优先级，忽略设计稿页头页脚；仅按我明确要求的范围修改，完成当前 Shopify 开发需求。"
policy:
  allow_implicit_invocation: true
